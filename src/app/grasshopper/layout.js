import React from "react";

export const metadata = {
  title: "GRASSHOPPER",
  description:
    "Grasshopper , Precision engineering for exquisite facade fabrication.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
