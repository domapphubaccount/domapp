import React from "react";

export const metadata = {
  title: "FACADESTATIC",
  description: "FACADESTATIC",
};

export default function RootLayout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
