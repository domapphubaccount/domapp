import React from "react";

export const metadata = {
  title: "WINDMASTER",
  description: "Windmaster , Calculate wind loads on structures.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
