import React from "react";

export const metadata = {
  title: "CLADCUT",
  description: "CLADCUT",
};

export default function RootLayout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
