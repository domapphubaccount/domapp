import React from "react";

export const metadata = {
  title: "BONDIFY CRM",
  description: "BONDIFY CRM , Project management, Invoicing, Leads, Tasks, Time tracking and more.",
};

export default function RootLayout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
