import React from "react";

export const metadata = {
  title: "BONDIFY CRM",
  description: "BONDIFY CRM",
};

export default function RootLayout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
