import React from "react";

export const metadata = {
  title: "RDAPP",
  description: "RDAPP",
};

export default function RootLayout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
