import React from "react";

export const metadata = {
  title: "RDAPP",
  description: "RDAPP",
};

export default function Layout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
