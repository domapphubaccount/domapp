import React from "react";

export const metadata = {
  title: "CLADCUT",
  description: "CLADCUT",
};

export default function Layout({ children }) {

  return (
    <>
        {children}
    </>
  );
}
